void main(){
  final name;
  name = 'Mahan';
  const a = 7; 
  // final variables can be initialised and declared seperately but const need to be declared at the time of initialisation.
  final names = ['Ram','Shyam','Mohan'];
  names.add('Peter'); // this would get added but if we had used const then it couldnt be appended
}